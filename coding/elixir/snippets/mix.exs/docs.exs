# Dependency to be added in mix.exs deps
# {:ex_doc, "~> 0.31", only: :dev, runtime: false}

# Configuration to be added in mix.exs project
[
  docs: [
    main: "readme",
    source_ref: "v<%= @version %>",
    source_url: "https://github.com/user/repo",
    extras: ["README.md"]
  ]
]
